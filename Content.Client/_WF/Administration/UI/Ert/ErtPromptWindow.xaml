<!-- Names must avoid DefaultWindow's own: CloseButton, ContentsContainer, TitleLabel, WindowHeader. -->
<DefaultWindow xmlns="https://spacestation14.io"
               Title="{Loc 'wf-ert-prompt-title'}"
               MinSize="400 0">
    <BoxContainer Orientation="Vertical" Margin="8 4">
        <Label Name="TeamLabel" StyleClasses="LabelHeading" />
        <RichTextLabel Name="BriefingLabel" HorizontalExpand="True" Margin="0 4" />
        <Label Name="PlacesLabel" StyleClasses="LabelSubText" />
        <BoxContainer Orientation="Horizontal" Margin="0 8 0 0">
            <Button Name="SignUpButton"
                    Text="{Loc 'wf-ert-prompt-sign-up'}"
                    StyleClasses="ButtonSquare"
                    HorizontalExpand="True" />
            <Button Name="LeaderButton"
                    Text="{Loc 'wf-ert-prompt-sign-up-leader'}"
                    StyleClasses="ButtonSquare"
                    HorizontalExpand="True"
                    Margin="4 0" />
            <Button Name="RolesButton"
                    Text="{Loc 'wf-ert-prompt-all-roles'}"
                    StyleClasses="ButtonSquare" />
        </BoxContainer>
        <Label Name="StatusLabel" StyleClasses="LabelSubText" Margin="0 4 0 0" />
    </BoxContainer>
</DefaultWindow>
